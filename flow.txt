attacks/fgsm.py              ← done
utils/metrics.py             ← accuracy, ASR, robustness gap functions
experiments/eval_clean.py    ← baseline clean accuracy with CSV saving
experiments/eval_robust.py   ← FGSM eval across compression levels
defenses/adversarial_training.py
experiments/eval_efficiency.py
notebooks/00_setup_check.ipynb
notebooks/02_results_viz.ipynb


Then: utils/profiler.py
Tell Claude Code:

"Read CLAUDE.md and configs/base.yaml, then write utils/profiler.py with two functions: measure_latency_ms (runs a model forward pass N times, discards warmup runs, returns median latency) and measure_peak_vram_mb (returns peak GPU memory in MB during a forward pass). Read warmup and measurement run counts from the config."


Then: experiments/eval_clean.py
Tell Claude Code:

"Read CLAUDE.md, configs/base.yaml, models/loader.py, and utils/metrics.py. Write experiments/eval_clean.py — a script that accepts --model and --compression as CLI args, loads the model, runs clean accuracy eval on the ImageNet validation subset, and saves results to results/eval_clean.csv using the resumability pattern from CLAUDE.md."


Then: experiments/eval_robust.py
Tell Claude Code:

"Read CLAUDE.md, configs/base.yaml, attacks/fgsm.py, and utils/metrics.py. Write experiments/eval_robust.py that accepts --model, --compression, and --attack as CLI args, runs FGSM on the validation subset, and saves clean_acc, robust_acc, asr, and robustness_gap to results/eval_robust.csv using the resumability pattern."


Then: defenses/adversarial_training.py
Tell Claude Code:
"Read CLAUDE.md, configs/base.yaml, models/loader.py, and attacks/fgsm.py. Write defenses/adversarial_training.py — a function that takes a loaded model and fine-tunes it using FGSM adversarial training for N epochs (from config). Save a checkpoint to results/checkpoints/ after every epoch. Print epoch loss and accuracy so progress is visible in Colab."

Once you have eval_clean.py done, that's actually your first Colab test — push to GitHub, clone on Colab, run it on DeiT-S FP32, and make sure you get a CSV with a real accuracy number before writing anything else.
Which of these do you want me to pre-generate so Claude Code has a reference?